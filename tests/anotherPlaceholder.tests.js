import { Meteor } from 'meteor/meteor';
import { assert } from 'chai';

describe('Another Placeholder Suite', function () {
  it('checks that true is true', function () {
    assert.isTrue(true, 'The expectation is that true is true');
  });
});
